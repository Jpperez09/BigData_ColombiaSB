# Streamlit Cloud secrets template — copy to .streamlit/secrets.toml and fill
# in real values when deploying. The actual secrets.toml is gitignored.
#
# For local dev these can also be set via the project root .env file.

SUPABASE_URL = "https://your-project.supabase.co"
SUPABASE_SERVICE_KEY = "your-service-role-key"
